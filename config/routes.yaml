controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

# Note: ReactController handles SPA routing with priority: -1
# No catch-all needed here as it would conflict with React routing